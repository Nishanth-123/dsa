#define ll int
class Solution {
public:
    int lengthOfLastWord(string s) {
        ll ans=0,e=s.size()-1;
        while(s[e]==' '){e--;}
        while(e>=0 && s[e]!=' '){ans++;e--;}
        return ans;
    }
};
